{
  "kind": "build_request",
  "title": "Add search and sort functionality for media entries",
  "priority": "normal",
  "requirements": [
    {
      "id": "REQ-1",
      "text": "Add a search input field above the media grid that filters media entries by title in real-time as the user types",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-latest"
        ],
        "quotes": [
          "add an option to search media"
        ]
      },
      "acceptanceCriteria": [
        "Search input appears above the media grid on the Dashboard page",
        "Typing in the search field filters visible media entries by matching title text (case-insensitive)",
        "Search results update in real-time as the user types",
        "Empty search shows all media entries",
        "Search preserves when switching between tabs or refreshing if applicable"
      ]
    },
    {
      "id": "REQ-2",
      "text": "Add a sort dropdown that allows users to sort media entries by: Title (A-Z), Title (Z-A), Rating (Highest First), Rating (Lowest First), and Recently Added",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-latest"
        ],
        "quotes": [
          "an option to sort"
        ]
      },
      "acceptanceCriteria": [
        "Sort dropdown appears above the media grid alongside the search input",
        "Dropdown includes options: Title (A-Z), Title (Z-A), Rating (Highest First), Rating (Lowest First), Recently Added",
        "Selected sort order immediately reorders the displayed media entries",
        "Sort order persists when search filters are applied",
        "Default sort order is Recently Added"
      ]
    }
  ],
  "constraints": [
    "Do not modify the backend API or data models",
    "Maintain the existing MediaCard and MediaList component structure",
    "Search and sort must work together (search filters, then sort orders the filtered results)"
  ],
  "nonGoals": [
    "Advanced search filters by media type or rating range",
    "Backend-side search or sorting implementation",
    "Saving user's search or sort preferences across sessions"
  ],
  "imageRequirements": {
    "required": [],
    "edits": []
  }
}